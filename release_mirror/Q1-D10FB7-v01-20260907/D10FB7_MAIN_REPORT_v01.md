
# D10FB7 Local Identity Preflight v01

## Executive answer

### Singleton

**Can the exact boundary `state + evaluation + fold + source EU` supply local variance-information material for all 24 fold-induced singletons? NO.**

The frozen plot-block crosswalk recovers all 24 target parents at exactly `n_h=1`. Under that one deterministic boundary, 11/24 cases are `STRUCTURALLY_AVAILABLE` and 13/24 are `STRUCTURALLY_ISOLATED`; 0 are identity-incomplete. Available cases: `S01;S02;S03;S04;S05;S12;S16;S21;S22;S23;S24`. Isolated cases: `S06;S07;S08;S09;S10;S11;S13;S14;S15;S17;S18;S19;S20`.

This is a material-availability result only. It does not select a donor, define similarity, pool observations, choose weights, estimate variance, or qualify a method. The negative universal answer means that the same-source-EU boundary cannot close the singleton interface for all 24 cases.

### WV

**Current frozen-sample result: `NO LOCAL EXCEPTION EVIDENCE`.**

All 13/13 current WV `EU3+EU4` plot visits were joined exactly across B5, D10C-A, and the frozen local SQLite. Every row has `DESIGNCD=1` (national design), `KINDCD=2` (remeasurement of a national-design plot), `QA_STATUS=1` (standard production plot), `INTENSITY=1` (FIA base grid), and `SUBPANEL=0`. No current row carries a non-base intensity or special-design identity.

The WV survey notes do disclose 3x intensification on named National Forest lands. B7 does not erase that context. The bounded finding is narrower: all 13 selected Inland Census Water EU3/EU4 rows themselves have `INTENSITY=1`, so the current frozen sample contains no row-level intensification/local-augmentation exception.

### Fold-B EU3 zero

**Panel-zero result: `NO LOCAL STRUCTURAL RESTRICTION EVIDENCE`.**

The two current EU3 rows are both `P2PANEL=5`; frozen fold B selects panels `1,2,4`, so its observed EU3 count is zero. EU4 supplies five fold-B rows from panels 1 and 2. No row-level local exception or panel-by-EU restriction flag distinguishes EU3. This supports a realized panel-composition explanation and does not prove structural exclusion. The strict counterfactual inclusion-probability identity remains open exactly as B6 stated.

## A. Singleton source-EU pooling preflight

- Case identities recovered: 24/24.
- `STRUCTURALLY_AVAILABLE`: 11.
- `STRUCTURALLY_ISOLATED`: 13.
- `IDENTITY_INCOMPLETE`: 0.
- Same deterministic boundary covers all 24: **NO**.
- Candidate non-singleton poststrata per case: min 0, median 0, max 149.
- Candidate non-singleton plots per case: min 0, median 0, max 5350.
- Residual-df proxy `sum(n_h-1)` over candidate non-singleton poststrata: min 0, median 0, max 5201.

The detailed CSV lists every qualifying poststratum identity and its fold count, but the list is an inventory, not a donor assignment. All qualifying contributors are inside the source EU by construction. WA source-EU scopes show `DESIGNCD` heterogeneity (`501;502`); this is reported rather than normalized away. All target scopes retain `R0` repair identity.

## B. WV production-identity audit

The 13 current rows divide into EU3=2 and EU4=11. EU3 has panels `5:2`; EU4 has panels `1:3;2:2;3:3;5:3`. Protocol manual values differ because the current rows span different inventory years, but all `MANUAL>=1` and all rows share national design, base-grid intensity, and standard-production QA identity.

Source production expansion and subplot adjustment fields differ between EU3 and EU4 (`official_full_expns` and `adj_factor_subp`). B7 records those differences. It does not reinterpret them as selection-intensity exceptions, and it does not alter the frozen common pooled coefficient.

## C. Status discipline

- B3, B5, and B6 are accepted upstream results with the relay/package identities recorded in `D10FB7_INPUT_IDENTITIES_v01.csv`.
- B6 remains controlling authority: singleton exact grouping is unresolved; WV current-production support is not strict randomization reconstruction.
- A2 remains frozen.
- D10F-C is not authorized.
- No real target-species outcome, TREE/SPCD table, donor, variance estimate, synthetic world, covariance, or external search was used.

STOP after B7. Return to Q1 mainline.
